<nav class="navbar navbar-expand-lg navbar-light">
  <%= link_to 'GURI', root_path, class: 'navbar-brand' %><i class="fab fa-dev dev-size"></i>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="#">Teste<span class="sr-only">(current)</span></a>
      </li>
      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to 'Painel do Usuário', users_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <a class="nav-link"><%= current_user.email %></a>
      </li>
      <% end %>
      <li class="nav-item">
          <div class="form-group mx-sm-3 mb-2">
          <%= form_tag search_path, method: :get, class: 'form-inline' do %>
            <%= label_tag :search, 'Busca', class: 'sr-only' %>
            <%= text_field_tag :search %>
            <%= submit_tag 'Buscar', class: 'btn btn-outline-success my-2 my-sm-0' %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
